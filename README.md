🔄 Git Workflow
1. Вносите изменения в файлы  
2. git add . — добавляете изменения в индекс  
3. git commit -m "описание" — фиксируете локально  
4. git pull origin ветка — обновляетесь с удалённого репозитория  
5. git push origin ветка — публикуете изменения  

---

🏗️ MVT в Django
• Model — структура данных и работа с БД  
• View — логика обработки запроса  
• Template — HTML-шаблон для отображения  

Как работает:  
Запрос → View (берёт данные из Model) → Template → HTML-ответ  

---

🗃️ JOIN в SQL
• INNER JOIN — только совпадающие строки из обеих таблиц  
• LEFT JOIN — все строки слева + совпадения справа (NULL если нет)  
• RIGHT JOIN — все строки справа + совпадения слева (NULL если нет)  
• FULL JOIN — все строки из обеих таблиц (NULL где нет совпадений)  
• CROSS JOIN — все комбинации строк (декартово произведение)  

Проще:  
• INNER = пересечение  
• LEFT/RIGHT = одна таблица полностью + вторая по совпадениям  
• FULL = объединение  
• CROSS = умножение строк
